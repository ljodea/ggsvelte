// SvelteKit ambient types (global declaration file — no imports/exports, so
// no `declare global` wrapper is needed). The $examples alias is declared in
// svelte.config.js and resolved by svelte-kit sync's generated tsconfig.
declare namespace App {
  // interface Error {}
  // interface Locals {}
  // interface PageData {}
  // interface PageState {}
  // interface Platform {}
}
