{
  "name": "@ggsvelte/docs",
  "version": "0.0.0",
  "private": true,
  "description": "ggsvelte docs site: SvelteKit + adapter-static. The gallery and per-example pages render from examples/manifest.ts (one source, three uses); the built output in build/ is the VR screenshot target.",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "bun ../../scripts/gen-docs-routes.ts --check && bun ../../scripts/gen-docs-search.ts --check && bun ../../scripts/gen-lesson-charts.ts --check && bun ../../scripts/gen-gallery-previews.ts --check && bun ../../scripts/gen-theme-static-shells.ts --check && svelte-kit sync && vite build && bun ../../scripts/check-docs-metadata.ts && bun ../../scripts/docs-csp.ts",
    "preview": "vite preview",
    "check": "bun ../../scripts/gen-docs-routes.ts --check && bun ../../scripts/gen-docs-search.ts --check && bun ../../scripts/gen-lesson-charts.ts --check && bun ../../scripts/gen-gallery-previews.ts --check && bun ../../scripts/gen-theme-static-shells.ts --check && svelte-kit sync && svelte-check --fail-on-warnings",
    "test": "svelte-kit sync && vitest run"
  },
  "dependencies": {
    "@ggsvelte/core": "workspace:*",
    "@ggsvelte/examples": "workspace:*",
    "@ggsvelte/spec": "workspace:*",
    "@ggsvelte/svelte": "workspace:*",
    "bits-ui": "^2.18.1"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.70.0",
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@testing-library/svelte-core": "^1.1.3",
    "@vitest/browser-playwright": "^4.1.10",
    "playwright": "1.61.1",
    "svelte": "^5.56.7",
    "svelte-check": "^4.7.3",
    "typescript": "^6.0.3",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
