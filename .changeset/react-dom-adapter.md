---
"@ggsvelte/compose": minor
"@ggsvelte/react": minor
"@ggsvelte/svelte": patch
---

Add `@ggsvelte/react`, a React DOM adapter over the same core pipeline and ggplot2 names as `@ggsvelte/svelte`. Shared spec assembly lives in `@ggsvelte/compose`.

Migration: none — additive
