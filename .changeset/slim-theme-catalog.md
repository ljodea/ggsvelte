---
"@ggsvelte/core": patch
---

Headless `runPipeline` now ships only the `default` and `void` theme tables. Named themes still resolve from `@ggsvelte/core` and `@ggsvelte/core/render` with no new caller argument.
