{
  "title": "Regular density grid (geom raster)",
  "description": "Equal-cell raster from a regular (x, y) grid with fill — dense heatmaps without per-cell strokes. The grid must be complete, so the source's 22 × 42 table is cropped to its largest complete 15 × 33 sub-rectangle.",
  "tags": ["raster", "grid", "heatmap"],
  "docsSection": "Rectangles & grids"
}
