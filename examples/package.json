{
  "name": "@ggsvelte/examples",
  "version": "0.0.0",
  "private": true,
  "description": "Shared example corpus: one source feeding the docs gallery, the VR matrix, and llms-full.txt (plan: 'one source, three uses'). Each example is <category>/<name>/{spec.ts, Example.svelte, meta.json, data.ts?}; examples/manifest.ts is GENERATED by scripts/gen-manifest.ts.",
  "type": "module",
  "dependencies": {
    "@ggsvelte/spec": "workspace:*",
    "ggsvelte": "workspace:*"
  },
  "devDependencies": {
    "svelte": "^5.46.4",
    "typescript": "^6.0.3"
  }
}
