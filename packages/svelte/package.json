{
  "name": "@ggsvelte/svelte",
  "version": "0.35.0",
  "description": "ggsvelte: a layered grammar of graphics for Svelte 5 — ggplot2 semantics, JSON-serializable specs, agent-friendly validation, and hybrid SVG/canvas rendering. The ggsvelte-render CLI ships separately as @ggsvelte/cli.",
  "keywords": [
    "charts",
    "components",
    "dataviz",
    "ggplot2",
    "grammar-of-graphics",
    "interactive",
    "plot",
    "svelte",
    "svelte5",
    "visualization"
  ],
  "homepage": "https://ggsvelte.sh/",
  "bugs": "https://github.com/ljodea/ggsvelte/issues",
  "license": "MIT",
  "author": "Liam O'Dea",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ljodea/ggsvelte.git",
    "directory": "packages/svelte"
  },
  "bin": {
    "ggsvelte-codemod": "bin/ggsvelte-codemod.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "svelte": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./data": {
      "types": "./dist/data/index.d.ts",
      "default": "./dist/data/index.js"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "svelte-package -i src/lib -o dist",
    "check": "svelte-check --fail-on-warnings",
    "test": "bun run test:browser && bun run test:ssr",
    "test:browser": "vitest run",
    "test:ssr": "vitest run --config vitest.ssr.config.ts",
    "test:coverage": "vitest run --coverage --project chromium && vitest run --coverage --config vitest.ssr.config.ts"
  },
  "dependencies": {
    "@ggsvelte/core": "^0.35.0",
    "@ggsvelte/spec": "^0.35.0"
  },
  "devDependencies": {
    "@sveltejs/package": "^2.5.8",
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@testing-library/svelte-core": "^1.1.3",
    "@vitest/browser-playwright": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.10",
    "axe-core": "^4.11.1",
    "playwright": "1.61.1",
    "svelte": "^5.56.7",
    "svelte-check": "^4.7.4",
    "typescript": "^6.0.3",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "svelte": "^5.33.1"
  },
  "engines": {
    "node": ">=22"
  }
}
