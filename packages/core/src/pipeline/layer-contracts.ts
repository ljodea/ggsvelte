/**
 * Per-layer pipeline contracts: render backends, tooltip field maps, and
 * scaled-constant values used by legend-focus indexing.
 */
export { resolveLayerBackends } from "./layer-backends.js";
export { resolveLayerFields, resolveLayerScaledConstants } from "./layer-fields.js";
