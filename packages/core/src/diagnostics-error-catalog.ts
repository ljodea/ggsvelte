/**
 * Pipeline error catalog — re-exported from @ggsvelte/spec (#987).
 * Emission sites stay in core; prose ownership is in the spec package so
 * docs and agents have one source. Do not add catalog entries here.
 */

export {
  PIPELINE_ERROR_CATALOG,
  type PipelineErrorCatalogEntry,
  type PipelineErrorCode,
} from "@ggsvelte/spec";
